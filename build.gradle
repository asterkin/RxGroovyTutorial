subprojects {
  apply plugin: 'groovy'
  apply plugin: 'eclipse'

  repositories {
    mavenCentral()
  }

  dependencies {
    runtime 'com.netflix.rxjava:rxjava-core:0.16.1'
    runtime 'com.netflix.rxjava:rxjava-groovy:0.16.1'
  }

  version = '1.0'
}

